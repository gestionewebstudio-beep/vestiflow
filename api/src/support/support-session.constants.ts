/** Durata sessione assistenza operatore → tenant cliente. */
export const SUPPORT_SESSION_TTL_MS = 2 * 60 * 60 * 1000;

/** Header HTTP inviato dal frontend durante la sessione assistenza. */
export const SUPPORT_SESSION_HEADER = 'x-vestiflow-support-session';

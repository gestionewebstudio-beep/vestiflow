# Ripresa — 3 settembre 2026

> ⏳ **Documento temporaneo, non committato.** Serve alla chat che riprende il lavoro.
> Quando il lavoro riparte, si cancella: la verità sta nei documenti citati in fondo.

## Dove siamo

Ramo **`feature/pagamenti-tesoriera`**, allineato a `origin` su **`30f36aa2`**. Quattordici
commit sul remoto, cinque fatti oggi:

```
30f36aa2  fix(shopify): il pull rispetta lo spegnimento e collega la variante base
685948b5  docs(qualità): ferma duplicazioni e ampliamenti prima del commit
4ef79625  docs(pagamenti): la specifica del 21/08 sostituisce l'analisi
46e50867  feat(shopify): il Nome Shopify si separa dal nome interno
0170a770  feat(shopify): i prodotti importati si modificano
```

Suite tutte verdi al push: build API, `test:everything` (copertura 86,38 / 81,02 / 81,92 /
86,76 su soglie 76/69/71/76), build frontend, 72 prove d'integrazione, 312 Shopify.

## Che cosa è stato fatto

- **I prodotti importati da Shopify si modificano** (docs/24 §1.8): lock rimosso da client e
  API. Resta apposta la guardia sull'**eliminazione**.
- **Primo pezzo della Tranche 2** (§1.6): il push di un prodotto già collegato passa da
  GraphQL — `productUpdate`, `productVariantsBulkUpdate`, `productUpdate(media:)`. API fissata
  a **2026-07**. Nessun fallback REST; la **creazione** resta sul vecchio percorso finché non
  arriva `productSet`.
- **«Nome Shopify»** separato dal nome interno (§1.9): `Product.name` è solo VestiFlow,
  `shopifyTitle` è bidirezionale. Migration `products.shopify_title` applicata su database
  condiviso e di test.
- **Spegnere la sincronizzazione è reversibile** (§1.10): se l'archiviazione su Shopify non
  riesce, la disattivazione si annulla e le giacenze riprendono.
- **E ferma davvero anche il pull**: a interruttore spento un webhook o un import massivo non
  toccano il prodotto — la guardia sta nei due punti d'ingresso, **prima** dell'arricchimento,
  perché nel pull massivo un fallimento di `enrichProduct` scriveva l'errore addosso al
  prodotto spento senza mai raggiungere la guardia interna.
- **Il Nome Shopify arriva anche sui prodotti nati in VestiFlow**: il pull ne aggiorna
  esclusivamente `shopifyTitle`, senza toccare `Product.name` né sbloccare gli altri campi.
- **Specifica Pagamenti**: quella del 21/08 sostituisce l'Analisi v3.0, con **sedici voci
  recuperate** prima di cancellare il vecchio (marcate nel testo).

## ✅ La decisione dei 12 prodotti è stata presa — 03/09/2026

Erano 12 prodotti su 18 a non sincronizzarsi più: una variante locale senza SKU, senza barcode
e senza opzioni, e dall'altra parte la variante remota `Default Title` altrettanto nuda —
nessuno dei tre criteri aveva una chiave da confrontare.

⭐ **Il quarto criterio esiste**: una locale libera, una remota libera, entrambe senza dati che
le distinguano → si collegano. Basta uno SKU, un barcode, un'opzione vera o più di una candidata
per lato e il criterio non si applica: il push torna a fermarsi con l'errore visibile.

⚠️ **Non è stato ancora verificato sullo shop**: le prove sono unitarie. Il prossimo passo qui
sotto vale anche per questo.

## ⛔ Le decisioni che aspettano il proprietario

**1 · Un media Shopify in stato `FAILED`** prende comunque il suo id, quindi non viene
ricaricato e nessuno se ne accorge: l'immagine non arriva sulla vetrina in silenzio. Va deciso
se leggere `status` e segnalarlo.

**2 · Le voci indebolite della specifica Pagamenti**, segnalate e non toccate perché fuori
dall'ambito autorizzato: il ramo `feature/cassa` passa da vincolo ripetuto in cinque punti a un
inciso solo e sparisce da P0; «somma rate = totale **salvo compensazioni esplicite**» diventa
coincidenza esatta; il saldo di periodo non è fra le colonne del registro.

## Il prossimo passo, dichiarato dal proprietario

> «La tranche non è ancora considerata verificata su Shopify: dopo il commit faremo una prova
> reale e circoscritta sullo shop di sviluppo.»

Nella scheda prodotto il campo **«Nome Shopify»** si vede solo con Shopify attivo, sotto il
nome prodotto, con il comando «Copia nome VestiFlow».

## Quattro cose che costano se si ignorano

| | |
| --- | --- |
| **Il database è condiviso** col collega | solo `npm run prisma:deploy`. Mai `migrate dev`, `db push`, `reset`, `migrate diff --from-schema-datasource` |
| **La produzione riscrive il database** | i webhook vanno a Railway, che gira `main` sullo **stesso** database: dopo un push dal ramo, un campo può tornare indietro entro un secondo. Non è il ramo — vedi la memoria `webhook-shopify-vanno-in-produzione` |
| **Le integrazioni vogliono Docker** | `npm run db:test:up` + `prisma:deploy:test` + `test:integration`. ⛔ mai `down -v` |
| **Il pre-push esegue TRE comandi** | `npm run build --prefix api` (l'unico type-check del backend), `test:everything`, `build` |

⚠️ **`SHOPIFY_API_VERSION=2026-07`** è in `.env.example` e in locale: **Railway e gli altri
ambienti vanno aggiornati a mano.**

✅ **SUPERATA** — `check:nome-fattura` è stato corretto dal commit `61032747`.
`npm run lint` percorre ora **tutte** le guardie fino all'ultima ed è verde: non serve più
lanciarle una per una.

⛔ Qui c'era: «resta un difetto preesistente che blocca `npm run lint` a metà:
`check:nome-fattura` è rosso già su `HEAD` per un'occorrenza nella guida tecnica, e i 30
check successivi non vengono eseguiti». Non vale più.

## Dove sta la verità

| Documento | Che cosa contiene |
| --- | --- |
| `docs/24-specifica-ciclo-vita-catalogo-e-sincronizzazione-shopify-v2.md` | §1.6 Tranche 2 · §1.8 interruttore e importati · §1.9 Nome Shopify · §1.10 spegnimento · §16 tabella delle tranche con lo stato |
| `docs/DA-FARE.md` | la lista generale, con in testa la sezione Shopify e la decisione dei 12 prodotti |
| `.claude/rules/regole-gestionale.md` | l'ownership dei dati, con le due eccezioni decise oggi |
| `docs/VestiFlow_Specifica_Pagamenti_Tesoreria_v1_1_21-08-2026_CLAUDE.md` | il modulo Pagamenti, con i sedici recuperi marcati |

## Dopo il merge in develop

Stato al **04/09/2026**: la **PR #1 è interamente verde** sull'ultimo SHA
`62a8e93b` — Lint & unit tests, Playwright E2E, Lighthouse CI, Audit dipendenze,
RLS exposure check. E2E e Lighthouse sono verdi **due volte**, con id di job
diversi fra il primo tentativo e il rilancio.

| Vincolo | |
| --- | --- |
| **La PR si unisce SOLO in `develop`** | ⛔ oggi punta a `main`: il bersaglio va cambiato **prima** di unire |
| **`main` non si modifica** | in nessun modo |
| **`feature/pagamenti-tesoriera` non si cancella subito** | resta dopo l'unione |

### 1. Lo stesso difetto delle date esplicite, in altri due elenchi

Corretto oggi su `document-list.component.ts`: con `dateFrom`/`dateTo` nell'URL il
preset nasce `Custom`, e il costruttore chiamava `resolveMovementPeriodRange(Custom,
'', '')` — che a `Custom` risponde con gli estremi che le si passano, cioè
`{ from: undefined, to: undefined }` — cancellando le due date che l'avevano resa
`Custom`.

**Lo stesso ramo, identico, è in altri due elenchi:**

| File | Righe | Parametri nell'URL |
| --- | --- | --- |
| `src/app/features/orders/supplier-order-list.component.ts` | ~317 e ~370 | `dateFrom` / `dateTo` |
| `src/app/features/sales-orders/sales-order-list.component.ts` | ~719 e ~833 | `placedFrom` / `placedTo` |

⚠️ **I nomi dei parametri NON sono gli stessi nei due file**: l'ordine fornitore usa
`dateFrom`/`dateTo` come il registro documenti, l'ordine cliente `placedFrom`/`placedTo`.
Cercare la stringa sbagliata fa mancare metà del lavoro.

⛔ **Nessun e2e copre quei due elenchi**, quindi il difetto è muto: non lo trova nessun
controllo automatico.

⭐ **Prima di correggere, verificare se la regola vada CENTRALIZZATA.** Sono tre punti che
applicano la stessa decisione — è la soglia oltre la quale `regole-qualita`
(«DUPLICAZIONE E PERIMETRO») chiede di fermarsi e valutare una funzione comune, invece di
scrivere la stessa condizione una terza volta.

### 2. I 21 riferimenti a una sezione che non esiste

`§H14-bis` **non esiste** in `docs/14-specifica-elenchi-documenti.md`. Il contratto —
apertura sugli ultimi 30 giorni, date effettive nell'URL con Periodo attivo, date rimosse
con `Tutti` — è la **§28 «Riepiloghi operativi — contratto specifico»**.

La sigla sbagliata è citata in **21 punti di 12 file** (`src/` e `api/`). Si sostituiscono
**tutti insieme**: oggi funziona come etichetta ricercabile di quella decisione, e
correggerne una parte spezzerebbe la ricerca senza sistemare niente.

```
rg "H14-bis" --glob '!node_modules' --glob '!coverage'
```

### 3. Audit dipendenze: non toccare il timeout adesso

Il job è scaduto una volta al tetto di `timeout-minutes: 10`, e la causa è esterna: il
passo bloccante `npm audit --omit=dev` ha impiegato **8 min 19 s** alle 22:26 contro
**21 s** alle 21:57 dello stesso giorno, **con lo stesso lockfile**. Rilanciato, è passato.

⛔ **Non aggiungere un timeout al comando né alzare `timeout-minutes`** per un guasto che
non è del controllo. Si interviene **solo se la lentezza si ripete**.

### 4. Poi si riprende Shopify

Nell'ordine: **Tranche 2B**, e la **progettazione della prima sincronizzazione**. Lo stato
delle tranche è in `docs/24` §16.

---

## `feature/cassa` — verificato il 04/09/2026, e NON eliminato

> ⛔ **Il ramo NON e' stato cancellato, e non va cancellato.** Le sue sei migration
> sono **tutte applicate nel database condiviso**: buttare il ramo lascerebbe nel
> database sei migration senza il codice che le corrisponde.

**HEAD del ramo**: `6e4f9e79ca5bc49a0506719d8de8e56443a17fd6`
(19/08/2026 21:02, «fix(cassa): l'allineamento a develop lascia il ramo compilabile e verde»)

Diverge da `develop` a `24543b30`; e' indietro di **463 commit** e porta **79 file**
(59 aggiunti, 20 modificati, +6.918 / -43 righe). **Nessuna PR** aperta o chiusa lo
riguarda: nell'intero repository esiste solo la #1, di un altro ramo.

### I sette commit che si perderebbero

```
6e4f9e79  19/08 21:02  fix(cassa): l'allineamento a develop lascia il ramo compilabile e verde
1efd5e4c  19/08 20:53  Merge branch 'develop' into feature/cassa
577235db  07/08 01:12  feat(cassa): anagrafica terminali POS, retry sul push canali, pannello pagamenti estratto
e6e01d0f  07/08 00:27  feat(cassa): emissione del documento commerciale via stampante Epson RT
762b0439  06/08 23:59  feat(cassa): le vendite di negozio entrano nel registro Corrispettivi
8ece224d  06/08 23:37  feat(cassa): sessioni di cassa con chiusure e movimenti di cassetto
fb35a2e3  06/08 23:03  feat(cassa): pagamenti multi-metodo e fondazioni del dispositivo fiscale
```

### Le sei migration del ramo — e il loro stato REALE nel database

⛔ **Tutte e sei risultano applicate**, `finished_at` valorizzato, `rolled_back_at` nullo,
`applied_steps_count` 1. Interrogazione in sola lettura di `_prisma_migrations`
(147 migration registrate in totale), 04/09/2026:

```
20260806120000_cassa_pagamenti_e_dispositivi_fiscali   applicata 06/08 20:30
20260806220000_sessioni_di_cassa                       applicata 06/08 21:08
20260806233000_corrispettivi_canale_cassa              applicata 06/08 21:45
20260806233500_backfill_corrispettivi_cassa            applicata 06/08 21:45
20260807003000_fiscal_device_reparti                   applicata 06/08 22:00
20260807010000_pos_terminals                           applicata 06/08 22:31
```

Nessuna migration del database risulta incompleta o annullata, in generale.

⚠️ **Ne discende una conseguenza che vale oltre questo ramo**: il database condiviso
porta gia' le tabelle della cassa — `cash_sessions`, `fiscal_receipts`, `pos_terminals`,
`store_sale_payments` e i campi collegati. E' la stessa cosa che `regole-qualita`
registra nella sezione sui comandi Prisma vietati, dove `migrate diff` proponeva di
cancellarle. Sono li' anche se la cassa non e' in `develop`.

### Le funzionalita' che il ramo porta

- **pagamenti multi-metodo** sulla vendita di negozio, con la relativa utilita' di ripartizione
- **dispositivo fiscale**: emissione del documento commerciale via **stampante Epson RT**,
  payload di stampa, reparti fiscali, esiti
- **sessioni di cassa**: apertura, chiusura, movimenti di cassetto, totali
- **registro Corrispettivi** alimentato dalle vendite di negozio (canale cassa)
- **terminali POS**: anagrafica, finestra del portale, pannello di pagamento
- **retry con backoff** sul push verso i canali
- pannelli in Impostazioni per dispositivo fiscale e terminali POS

### Configurazioni e riferimenti esterni

Nessun file di configurazione o ambiente e' toccato dal ramo, e nel diff non compare
alcuna variabile di segreto. **Un riferimento da conservare c'e'**: l'indirizzo della
stampante fiscale Epson, `https://192.168.1.50/cgi-bin/fpmate.cgi`, con lo schema SOAP
`http://schemas.xmlsoap.org/soap/envelope/`.

### Se un giorno si decidesse comunque di lasciarlo andare

Prima va deciso **cosa fare delle sei migration gia' applicate**: o restano nel database
con le tabelle inutilizzate, o servono migration di rimozione scritte a mano. Non e' una
conseguenza della cancellazione del ramo: e' una decisione a se'.

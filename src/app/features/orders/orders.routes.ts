import { Routes } from '@angular/router';

import { OrdersComponent } from './orders.component';

export const ordersRoutes: Routes = [
  { path: '', title: 'VestiFlow · Ordini Fornitori', component: OrdersComponent },
];
